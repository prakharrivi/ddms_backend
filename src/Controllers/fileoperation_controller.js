var express = require('express')
var router = express.Router()
// const auth = require("../middleware/auth");
//  var mailsender = require('../../Routes/sendMail');
var bodyParser = require('body-parser')

var fileoperationService = require('../services/fileOperation_service')
var Constant = require('../Constant/Constant')

router.post('/workflow', async function (req, res) {
  fileoperationService
    .workflow(req)
    .then(function (data) {
      if (data) {
        res.status(200).send(data)
      } else {
        res.status(204).send(Constant.noRecordMessage)
      }
    })
    .catch((err) =>
      res.status(500).send({
        respCode: Constant.respCode500,
        respMessage: err.message,
      }),
    )
})

router.post('/allworkflow', async function (req, res) {
  fileoperationService
    .allworkflow(req)
    .then(function (data) {
      if (data) {
        res.status(200).send(data)
      } else {
        res.status(204).send(Constant.noRecordMessage)
      }
    })
    .catch((err) =>
      res.status(500).send({
        respCode: Constant.respCode500,
        respMessage: err.message,
      }),
    )
})

router.get('/superadmincall', async function (req, res) {
  fileoperationService
    .superadmincall(req)
    .then(function (data) {
      if (data) {
        res.status(200).send(data)
      } else {
        res.status(204).send(Constant.noRecordMessage)
      }
    })
    .catch((err) =>
      res.status(500).send({
        respCode: Constant.respCode500,
        respMessage: err.message,
      }),
    )
})

router.post('/loginuserdata', async function (req, res) {
  fileoperationService
    .loginuserdata(req)
    .then(function (data) {
      if (data) {
        res.status(200).send(data)
      } else {
        res.status(204).send(Constant.noRecordMessage)
      }
    })
    .catch((err) =>
      res.status(500).send({
        respCode: Constant.respCode500,
        respMessage: err.message,
      }),
    )
})

module.exports = router
